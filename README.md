# TTF_GlyphMetrics32
sample code to extract bounding box of a UTF8 character

Usage:

```sh
./a.out --font-file ./1.ttf --font-size 32 --utf8-text "Google" --show-box --show-baseline --show-single-texture
```
